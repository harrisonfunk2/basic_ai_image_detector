# basic_ai_image_detector
Basic AI image detector for scenic ocean photos. Uses a pretrained ResNet50 CNN as a feature extractor (2048-d embeddings) and a logistic regression classifier to predict whether an image is real or AI-recreated. Reports accuracy and ROC-AUC across repeated random splits.
